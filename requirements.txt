# ---- Phase 1: corpus ingestion ----
lxml==5.3.0                     # XML/DocBook parsing with XPath
tiktoken==0.8.0                 # token counting for corpus statistics

# ---- Phase 4: retrieval ----
rank-bm25==0.2.2                # lexical retrieval (BM25Okapi)
sentence-transformers==3.3.1    # embedding model runner
numpy==2.5.1                    # score arrays and RRF fusion

# chromadb 0.6.x depends on chroma-hnswlib, which ships no wheel for Python
# 3.13 and fails to build without Microsoft C++ Build Tools. 1.0.x replaced
# that C++ extension, so it installs cleanly on a stock interpreter.
chromadb==1.0.21

# ---- Phase 7: API ----
fastapi==0.115.6
uvicorn[standard]==0.34.0

# starlette 0.41.x accepts any anyio <5 and still imports
# `anyio.abc.BlockingPortal`, which anyio 4.15 deprecated. Importing
# fastapi.testclient then raises a DeprecationWarning, which this project's
# `filterwarnings = ["error"]` turns into a collection failure. Pinning anyio to
# a version contemporary with starlette is the honest fix; silencing the
# warning would hide a real version incompatibility.
anyio==4.7.0

# ---- Tooling ----
pytest==8.3.4
ruff==0.8.6
httpx==0.28.1                   # required by fastapi's TestClient
